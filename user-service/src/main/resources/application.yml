spring:
  application:
    name: user-service

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:file:./database/ecomdb
    username: sa
    password: password

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8082
